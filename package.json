{
  "name": "github-action-issue-to-jira",
  "private": true,
  "main": "dist/index.js",
  "scripts": {
    "start": "node ./index.js",
    "test": "jest",
    "build": "npx @vercel/ncc build && npx convert-action"
  },
  "dependencies": {
    "@actions/core": "^1.2.6",
    "actions-toolkit": "^2.2.0",
    "jira-client": "^6.14.0"
  },
  "devDependencies": {
    "jest": "^24.5.0",
    "nock": "^12.0.0"
  }
}
